import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.curbnturd.landing',
  appName: 'cnt-workspace',
  webDir: 'dist/cnt-workspace'
};

export default config;
